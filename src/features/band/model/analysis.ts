/**
 * Пороги, по которым сырьё превращается в выводы.
 *
 * Живут в ядре и выносятся в контракт намеренно. Пока они прятались в модели
 * экрана, любая сторонняя обработка тех же данных считала по своим числам, и
 * приложение показывало одну цифру, а сервер за тот же день — другую. Это
 * договорённость, а не деталь реализации: меняется она согласованно или не
 * меняется вовсе.
 */

/** Минута считается ходьбой от этого числа шагов: ниже — жесты рукой. */
export const WALKING_STEPS_PER_MINUTE = 20;

/** Человеческий диапазон длины шага в метрах. Вне его — расхождение окон, а не шаг. */
export const STRIDE_METRES = { min: 0.3, max: 1.2 } as const;

/** Ориентир длительности сна, к которому считается доля. */
export const SLEEP_TARGET_MINUTES = 8 * 60;

/** Границы зон пульса, уд/мин. Включительно с обеих сторон. */
export const HEART_RATE_ZONES = [
  { label: 'Rest', from: 0, to: 98 },
  { label: 'Warm-up', from: 99, to: 118 },
  { label: 'Fat burn', from: 119, to: 137 },
  { label: 'Aerobic', from: 138, to: 157 },
  { label: 'Anaerobic', from: 158, to: 177 },
  { label: 'Peak', from: 178, to: 250 },
] as const;

/** Границы зон стресса по шкале устройства. */
export const STRESS_ZONES = [
  { label: 'Relaxed', from: 1, to: 29 },
  { label: 'Normal', from: 30, to: 59 },
  { label: 'Medium', from: 60, to: 79 },
  { label: 'High', from: 80, to: 100 },
] as const;
